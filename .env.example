# Environment
NODE_ENV=development
API_URL=http://localhost:3000

# Database
DATABASE_URL=postgresql://nestuser:nestpassword@database:5432/nesttracker
# Optional: when running tests, if set, overrides DATABASE_URL
DATABASE_URL_TEST=postgresql://nestuser:nestpassword@localhost:5432/nesttracker_test
# Optional: per-test-file schema isolation (auto-generated by test setup if not provided)
# TEST_SCHEMA=t_abcd1234

# Optional: a separate .env.testing can be used during tests; values here override .env
# Create .env.testing next to this file for local test overrides.

# Frontend/Backend ports
FRONTEND_PORT=5173
BACKEND_PORT=3000

# JWT secret
JWT_SECRET=jwt-secret-key-here-it-must-be-at-least-32-characters-long

# Two-Factor Authentication
TWOFA_ENCRYPT_KEY=your-strong-encryption-key-here-it-must-be-at-least-32-characters-long
TWOFA_ENCRYPT_SALT=your-unique-salt-here-it-must-be-at-least-16-characters-long