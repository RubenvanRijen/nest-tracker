# Local testing environment variables
NODE_ENV=test

# Use a local Postgres instance for tests
# Adjust host/port/user/password/db as needed for your machine
DATABASE_URL_TEST=postgresql://nestuser:nestpassword@localhost:5432/nesttracker_test

# Required secrets (example weak defaults; replace locally if needed)
JWT_SECRET=local-test-jwt-secret-at-least-32-characters-long-1234
TWOFA_ENCRYPT_KEY=local-test-twofa-encryption-key-at-least-32
TWOFA_ENCRYPT_SALT=local-test-twofa-salt-123456
